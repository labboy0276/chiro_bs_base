{#
/**
 * @file
 * Default theme implementation to display a two-column layout.
 *
 * Available variables:
 * - content: The content for this layout.
 * - attributes: HTML attributes for the layout <div>.
 *
 * @ingroup themeable
 */
#}
{%
  set classes = [
    'layout',
    'layout--image-right',
    'row',
  ]
%}
{% if content %}
  <div{{ attributes.addClass(classes) }}>
    {% if content.text %}
      <div {{ region_attributes.text.addClass('layout-text', 'col-md-8') }}>
        {{ content.text }}
      </div>
    {% endif %}

    {% if content.image %}
      <div {{ region_attributes.image.addClass('layout-image', 'col-md-4') }}>
        {{ content.image }}
      </div>
    {% endif %}
  </div>
{% endif %}
